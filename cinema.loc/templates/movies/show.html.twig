{% extends 'base.html.twig' %}

{% block title %}Movies{% endblock %}

{% block body %}
    <h1>Movies</h1>

    <table class="table">
        <tbody>
            <tr>
                <td><h1>{{ movie.title }}</h1></td>
            </tr>
            <tr>
                <td>
                    <div class="col-md-4">
                        <div class="card mb-4 shadow-sm">
                            <img class="bd-placeholder-img card-img-top" width="100%" height="100%" src="{{ asset('img/') ~ movie.img }}" preserveAspectRatio="xMidYMid slice" focusable="false" role="img" aria-label="Plaeholder: Thumbnail"></img>
                        </div>
                    </div>
                </td>
            </tr>
            <tr>
                <td>{{ movie.description }}</td>
            </tr>
            <tr>
                <td>Available seanses: <br>
                    {% if is_granted('ROLE_USER') %}
                    <a href="{{ path('orders_new') }}">Order a ticket</a>
                    {% endif %}
{#                    {{ movie.seanses }}#}
                </td>
            </tr>
        </tbody>
    </table>

{#    {{ include('') }}#}


    <br><br>
    <a href="{{ path('movies_index') }}">back to list</a>

{#    <a href="{{ path('admin_movies_edit', {'id': movie.id}) }}">edit</a>#}

{#    {{ include('admin/movies/_delete_form.html.twig') }}#}

    <script src="{{ asset('js/hall.js') }}"></script>
{% endblock %}
